packages <- c(
"tidymodels", "tidyverse", "rio", "naniar", "labelled", "sjlabelled", "haven", "fixest", "vtable", "simputation",
"modelsummary", "gtsummary", "ggfixest", "patchwork"
)
install.packages(packages, repos = "http://cran.us.r-project.org") # Installing packages at once
packages <- c(
'tidymodels', 'tidyverse', 'rio', 'naniar', 'labelled', 'sjlabelled', 'haven', 'fixest', 'vtable', 'simputation',
'modelsummary', 'gtsummary', 'ggfixest', 'patchwork'
)
install.packages(packages, repos = 'http://cran.us.r-project.org') # Installing packages at once
lapply(packages, library, character.only = T) # Loading the packages
install.packages("tidyverse")
packages <- c(
'tidymodels', 'tidyverse', 'rio', 'naniar', 'labelled', 'sjlabelled', 'haven', 'fixest', 'vtable', 'simputation',
'modelsummary', 'gtsummary', 'ggfixest', 'patchwork'
)
install.packages(packages, repos = 'http://cran.us.r-project.org') # Installing packages at once
packages <- c(
'tidymodels', 'tidyverse', 'rio', 'naniar', 'labelled', 'sjlabelled', 'haven',
'fixest', 'vtable', 'simputation', 'modelsummary', 'gtsummary', 'ggfixest', 'patchwork'
)
# Install packages (only install missing ones)
new_packages <- packages[!(packages %in% installed.packages()[,"Package"])]
if(length(new_packages)) {
install.packages(new_packages, repos = 'http://cran.us.r-project.org')
}
# Load packages with better error handling
for(pkg in packages) {
if(!require(pkg, character.only = TRUE)) {
cat("Failed to load package:", pkg, "\n")
}
}
install.packages("gtsummary")
library(gtsummary)
install.packages("pacman")
library(pacman)
if (!require("pacman")) install.packages("pacman")
packages <- c(
'tidymodels', 'tidyverse', 'rio', 'naniar', 'labelled', 'sjlabelled', 'haven',
'fixest', 'vtable', 'simputation', 'modelsummary', 'gtsummary', 'ggfixest', 'patchwork'
)
pacman::p_load(char = packages)
pacman::p_load(char = packages)
git config --global user.email "adesai@gradcenter.cuny.edu"
l
git config --global user.email "adesai@gradcenter.cuny.edu"
git config --global user.email "adesai@gradcenter.cuny.edu"
git config --global user.email "adesai@gradcenter.cuny.edu"
